include ../layout
include ../footer

    block content
    h1 Books
    p
      a.button(href='books/new') Create New Book

    table
      thead
        tr
          th Title
          th Author
          th Genre
          th Year
      tbody
        tr
          td.capitalize
            input(class='search' type='text', name='fname')
          td.capitalize
            input(class='search' type='text', name='fname')
          td.capitalize
            input(class='search' type='text', name='fname')
          td.capitalize
            input(class='search' type='text', name='fname')
        each book in books
          tr
            td.capitalize
              a(href="/books/" + book.id) #{book.title}
            td.capitalize #{book.author}
            td.capitalize #{book.genre}
            td.capitalize #{book.year}

    block footer
            
            